# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/

# Node
node_modules/
npm-debug.log*

# 빌드 산출물
dist/
build/

# 로컬 환경
.env
.env.local

# OS / 에디터
.DS_Store
*.swp
.idea/
.vscode/

# ── 커밋하는 것 ──────────────────────────────────────────────
# tables/*.bin 은 **커밋한다.** 서버와 클라이언트가 같은 바이트를 봐야 하므로
# 생성 스크립트만 커밋하는 것으로는 부족하다. docs/simulation.md §3, tables/README.md
# tests/replays/*.jsonl 도 커밋한다. 골든 리플레이가 결정론의 회귀 기준선이다.

# 감사·디버깅용 임시 프로브 (에이전트가 만든다)
.probe/
