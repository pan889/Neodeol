"""순수 시뮬레이션. **아직 비어 있다 — Phase 3에서 만든다.**

`docs/roadmap.md` Phase 3: 이 패키지는 TypeScript 구현(`client/src/sim/`)의
**1:1 번역이며 창의성을 발휘하지 않는다.** 먼저 클라이언트 쪽을 정수화해
확정한 뒤(Phase 2) 여기로 옮긴다. 순서를 뒤집으면 두 구현이 갈라진다.

여기에 코드를 넣을 때의 절대 규칙 (CLAUDE.md):

1. `asyncio`, 소켓, 파일, 시계, `random.random()` 금지.
   입력은 `(state, inputs)`, 출력은 `new_state`.
2. 부동소수점 금지. 좌표는 정수 서브픽셀(`1px = 16 subpx`).
   나눗셈은 2의 거듭제곱 제수 + 산술 우측 시프트로만 한다.
3. 모래 자동자는 제안(propose) → 해소(resolve) 2단계로만 구현한다.
   셀을 순회하며 그 자리에서 옮기는 방식 금지.

Phase 0 기준 구현은 `tools/sandbox/index.html` 안에 있다. 그 파일의 `[SIM]`
구획이 명세를 실제로 만족하는 참조 구현이며, 자기검증 버튼 3개
(재현성 / 순서 독립성 / 동점 검출)가 그것을 실측한다.
"""
