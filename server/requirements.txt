# 상한을 열어두었다. 프로덕션 진입(Phase 7) 전에 lock 파일로 고정한다.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0      # uvloop + httptools 포함
msgpack>=1.1,<2.0
numpy>=2.1,<3.0                    # 모래 자동자 벡터화. 순수 파이썬 루프로는 519k 셀을 못 돈다
redis>=5.2,<7.0
asyncpg>=0.30,<1.0

# 개발/테스트
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
httpx>=0.28,<1.0
